from setuptools import setup

setup(
    name = 'dmap',
    version = '0.0.1',
    description = 'A dimensional mapping tool.',
    author = 'Nat Tomczak',
    license = 'MIT',
    packages = ['dmap'],
    python_requires = '>=3.7',
    install_requires = [], 
    extras_require = {}
)